version: 1
name: github
owner: data-platform
dbt_group: data_platform
description: Canonical validated GitHub entities and events for downstream data products.
curated_namespace: [curated, github]
upstream_sources: [github_archive]

tables:
  - key: events
    name: events
    description: Deduplicated and normalized GitHub events at one row per stable event ID.
    location_prefix: github/events
    schema_contract: github.events.v1
    primary_key: [event_id]
    partitioning:
      - field: event_date_utc
        transform: day
    write_mode: merge

  - key: actors_current
    name: actors_current
    description: Latest observed GitHub actor attributes at one row per actor ID.
    location_prefix: github/actors_current
    schema_contract: github.actors_current.v1
    primary_key: [actor_id]
    write_mode: merge

  - key: repositories_current
    name: repositories_current
    description: Latest observed GitHub repository attributes at one row per repository ID.
    location_prefix: github/repositories_current
    schema_contract: github.repositories_current.v1
    primary_key: [repository_id]
    write_mode: merge
