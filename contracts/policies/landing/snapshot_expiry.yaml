version: 1
name: mlh-landing-expire-snapshots
namespace: [landing]
owner: data-platform
policy_type: system.snapshot-expiry
description: Retain landing snapshots for thirty days.
content:
  version: "2025-02-03"
  enable: true
  config:
    max_snapshot_age_days: 30
targets:
  - type: namespace
    path: [landing]
