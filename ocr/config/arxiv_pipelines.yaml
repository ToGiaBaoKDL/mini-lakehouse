version: 1
default_pipeline: opendataloader_cpu

pipelines:
  opendataloader_cpu:
    processor: arxiv_opendataloader_pdf
    execution_backend: oci
  glm_ocr_modal:
    processor: arxiv_glm_ocr
    execution_backend: modal
