name: Release dbt Research

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/release-dbt-research.yml
      - dbt/Dockerfile
      - dbt/deploy/**
      - dbt/runtime/**
      - dbt/research/**
      - infra/runtime/identity/install-aws-signing-helper
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/_release-image.yml
    permissions:
      contents: read
      id-token: write
    with:
      component: dbt-research
      dockerfile: dbt/Dockerfile
      repository: tgbao-dev-dbt-research
      build_args: DBT_PROJECT=research
