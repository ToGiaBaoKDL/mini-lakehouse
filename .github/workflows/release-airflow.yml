name: Release Airflow runtime

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/release-airflow.yml
      - automation/airflow/pyproject.toml
      - automation/airflow/uv.lock
      - automation/airflow/runtime/**
      - automation/airflow/deploy/**
      - infra/runtime/postgres/**
      - infra/runtime/identity/install-aws-signing-helper
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/_release-image.yml
    permissions:
      contents: read
      id-token: write
    with:
      component: airflow
      dockerfile: automation/airflow/runtime/Dockerfile
      repository: tgbao-dev-automation-airflow
