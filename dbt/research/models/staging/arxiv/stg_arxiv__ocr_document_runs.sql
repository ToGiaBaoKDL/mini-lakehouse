select
    run_id,
    request_id,
    arxiv_id,
    oai_datestamp,
    source_record_sha256,
    pdf_url,
    pdf_sha256,
    pdf_size_bytes,
    page_count,
    processing_id,
    model_repository,
    model_revision,
    layout_model_repository,
    layout_model_revision,
    adapter_version,
    config_hash,
    artifact_uri,
    manifest_sha256,
    state,
    attempt,
    started_at,
    completed_at,
    curated_at,
    provider,
    provider_reference,
    provider_run_id,
    job_json
from {{ source('arxiv', 'ocr_document_runs') }}
