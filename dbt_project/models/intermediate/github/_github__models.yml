version: 2

models:
  - name: int_github__events_enriched
    description: >-
      Private, deduplicated GitHub events with commonly used payload attributes extracted once
      for all conformed downstream models.
    columns:
      - name: event_id
        data_tests: [not_null, unique]
      - name: source_hour
        data_tests: [not_null]
