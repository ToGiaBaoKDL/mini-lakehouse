select
    schema_version,
    feature_version,
    configuration_version,
    configuration_sha256,
    symbol,
    trade_date,
    decision_at,
    market_session,
    stream_session_id,
    last_receive_sequence,
    trade_age_seconds,
    quote_age_seconds,
    mid_price,
    microprice,
    microprice_deviation_bps,
    spread,
    spread_bps,
    bid_depth,
    ask_depth,
    level_one_imbalance,
    depth_imbalance,
    is_eligible,
    eligibility_reasons_json,
    available_at,
    processed_at,
    manifest_sha256,
    snapshot_sha256
from {{ source('t0_trading', 'feature_snapshots') }}
