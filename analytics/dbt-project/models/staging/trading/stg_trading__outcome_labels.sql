select
    schema_version,
    outcome_version,
    outcome_configuration_sha256,
    feature_version,
    feature_configuration_sha256,
    feature_snapshot_sha256,
    stream_session_id,
    symbol,
    trade_date,
    decision_at,
    action,
    horizon_seconds,
    order_quantity,
    entry_at,
    horizon_at,
    entry_quote_received_at,
    entry_receive_sequence,
    entry_vwap,
    horizon_quote_received_at,
    horizon_receive_sequence,
    horizon_vwap,
    gross_return_bps,
    is_eligible,
    outcome_reasons_json,
    processed_at,
    outcome_sha256
from {{ source('t0_trading', 'outcome_labels') }}
